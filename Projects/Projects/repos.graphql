query PublicRepos {
    viewer {
        login
        repositories(first: 10, privacy: PUBLIC) {
            edges {
                node {
                    name
                }
            }
        }
    }
}
